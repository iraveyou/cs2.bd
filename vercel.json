{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push --accept-data-loss && npm run build",
  "installCommand": "npm install",
  "regions": ["sin1"],
  "env": {
    "NODE_ENV": "production"
  }
}
